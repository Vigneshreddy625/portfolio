export default function Projects({ id }) {
    return (
      <section id={id} className="min-h-screen flex items-center justify-center">
        <h1 className="text-4xl font-bold">Welcome to My Website</h1>
        {/* Add more content for your home section */}
      </section>
    );
  }